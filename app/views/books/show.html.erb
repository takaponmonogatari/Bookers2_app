<div class="container">
  <div class="row">
    <div class="col-md-3">

        <%= render "users/list", book: @book_new, user: @user %>


    </div>
    <div class="col-md-8 offset-md-1">
<table class="table table-striped">
 <tr>
   <th></th>
   <th>Title</th>
   <th>Opinion</th>
   <th></th>
   <th></th>
 </tr><h1>Book detail</h1>

 <tr>
   <th><%= attachment_image_tag @book.user, :profile_image, :fill, 100, 100, format: 'jpeg', fallback: "no_image.jpg" %><br>
     <%= link_to user_path(@book.user.id) do %>
      <p><%= @book.user.name %></p>
     <% end %>

   </th>
   <th><%= link_to book_path(@user.id) do %>
    <p><%=@book.title %></p><% end %></th>
   <th><%= @book.body %></th>
    <% if @user.id == current_user.id %>
    <th><%= link_to "Edit", edit_book_path(@book.id) %></th>
    <th><%= link_to "Destroy", book_path(@book), method: :delete %></th>
  <% end %>
 </table>
 </div>
 </div>
</div>


