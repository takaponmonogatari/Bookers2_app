<table>
 <tr>
   <th>image</th>
   <th>name</th>
   <th></th>
 </tr><h1>Users</h1>


<% @users.each do |user| %>
<tr>
   <th><%= attachment_image_tag @user, :profile_image, :fill, 100, 100, format: 'jpeg', fallback: "no_image.jpg" %></th>
   <th>
    <%= user.name %>
   </th>
   <th><%= link_to "Show", user_path(user.id) %></th>
  </tr>

<% end %>
</table>